---
# Release creation is now handled by the Upload-Release job in build.yml.
# This workflow is kept as a no-op placeholder to avoid breaking references.
name: "tagged-release"

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release is created by the Package-Build workflow."
