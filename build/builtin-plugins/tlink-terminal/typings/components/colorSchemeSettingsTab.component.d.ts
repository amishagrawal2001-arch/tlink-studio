import { ConfigService, PlatformService } from 'tlink-core';
/** @hidden */
export declare class ColorSchemeSettingsTabComponent {
    config: ConfigService;
    defaultTab: string;
    constructor(platform: PlatformService, config: ConfigService);
}
