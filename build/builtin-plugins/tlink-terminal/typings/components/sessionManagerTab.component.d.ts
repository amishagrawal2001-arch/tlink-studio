import { Injector } from '@angular/core';
import { CdkDragDrop } from '@angular/cdk/drag-drop';
import { NgbModal } from '@ng-bootstrap/ng-bootstrap';
import { AppService, BaseTabComponent as CoreBaseTabComponent, NotificationsService, PartialProfile, PartialProfileGroup, PlatformService, Profile, ProfileGroup, ProfileProvider, ProfilesService, SelectorService, SidePanelService, TranslateService } from 'tlink-core';
import { BaseTerminalTabComponent } from '../api/baseTerminalTab.component';
declare const BaseTabComponentRuntime: typeof CoreBaseTabComponent;
interface SessionConnection {
    id: string;
    title: string;
    detail: string;
    protocol: string;
    isOpen: boolean;
    tab: BaseTerminalTabComponent<any>;
}
type SessionProfileGroup = PartialProfileGroup<ProfileGroup> & {
    collapsed?: boolean;
    profiles?: PartialProfile<Profile>[];
};
export declare class SessionManagerTabComponent extends BaseTabComponentRuntime {
    private app;
    private profiles;
    private sidePanel;
    private ngbModal;
    private notifications;
    private platform;
    private translate;
    private profileProviders;
    private selector;
    hostClass: boolean;
    get isSplitHost(): boolean;
    filterText: string;
    profileGroups: SessionProfileGroup[];
    connections: SessionConnection[];
    filteredConnections: SessionConnection[];
    filteredGroups: SessionProfileGroup[];
    visibleGroups: SessionProfileGroup[];
    selectedProfileIds: Set<string>;
    selectedGroupIds: Set<string>;
    viewMode: 'all' | 'connections' | 'built-in';
    activePanelId: string;
    private lastPanelId;
    private collapsedByGroupId;
    private connectionIdByTab;
    constructor(app: AppService, profiles: ProfilesService, sidePanel: SidePanelService, ngbModal: NgbModal, notifications: NotificationsService, platform: PlatformService, translate: TranslateService, profileProviders: ProfileProvider<Profile>[], selector: SelectorService, injector: Injector);
    ngOnInit(): void;
    get hasConnections(): boolean;
    get hasProfiles(): boolean;
    get showConnections(): boolean;
    get showProfiles(): boolean;
    get selectionCount(): number;
    get canEditSelection(): boolean;
    clearFilter(): void;
    onFilterTextChange(value: string): void;
    clearSelection(): void;
    toggleGroup(group: SessionProfileGroup): void;
    openGroupContextMenu(event: MouseEvent, group: SessionProfileGroup): void;
    openProfileContextMenu(event: MouseEvent, profile: PartialProfile<Profile>): void;
    selectGroup(group: SessionProfileGroup, event: MouseEvent): void;
    selectProfile(profile: PartialProfile<Profile>, event: MouseEvent): void;
    isGroupSelected(group: SessionProfileGroup): boolean;
    isProfileSelected(profile: PartialProfile<Profile>): boolean;
    focusConnection(connection: SessionConnection): void;
    openProfile(profile: PartialProfile<Profile>): Promise<void>;
    editSelected(): Promise<void>;
    deleteSelected(): Promise<void>;
    editProfile(profile: PartialProfile<Profile>): Promise<void>;
    deleteProfile(profile: PartialProfile<Profile>): Promise<void>;
    editGroup(group: SessionProfileGroup): Promise<void>;
    deleteGroup(group: SessionProfileGroup): Promise<void>;
    createProfile(group?: SessionProfileGroup): Promise<void>;
    private pickProfileProvider;
    private autoSaveQuickConnectProfile;
    private saveQuickConnectProfile;
    createGroup(): Promise<void>;
    closePanel(): void;
    private refreshProfiles;
    private sortProfilesForGroup;
    onProfileDrop(event: CdkDragDrop<PartialProfile<Profile>[]>, targetGroup: SessionProfileGroup, target: 'header' | 'list'): Promise<void>;
    private resolveDropIndex;
    private updateProfileOrder;
    private duplicateProfile;
    private refreshConnections;
    private buildConnection;
    private isBuiltInGroup;
    private isBuiltInPanel;
    private isRemoteDesktopPanel;
    trackByConnection(_index: number, connection: SessionConnection): string;
    trackByGroup(index: number, group: SessionProfileGroup): string | number;
    trackByProfile(index: number, profile: PartialProfile<Profile>): string | number;
    private getAllTerminalTabs;
    private findTabForProfile;
    private focusTab;
    private getConnectionId;
    private findConnectionTab;
    private get normalizedFilter();
    private matchesFilter;
    private profileMatchesFilter;
    private applyFilter;
    private pruneSelections;
    private get selectedProfiles();
    private get selectedGroups();
    private isMultiSelect;
    private showProfileEditModal;
    private showProfileGroupEditModal;
    private editProfileGroupDefaults;
    private cleanProfileGroup;
}
export {};
