import { OnDestroy, OnInit, ElementRef } from '@angular/core';
import { AppService, ConfigService, PartialProfile, ProfilesService, SidePanelService, TranslateService, NotificationsService } from 'tlink-core';
import { TerminalColorSchemeProvider } from '../api/colorSchemeProvider';
import { TerminalColorScheme } from '../api/interfaces';
export declare class ColorTemplatesPanelComponent implements OnInit, OnDestroy {
    profilesService: ProfilesService;
    private config;
    private sidePanel;
    private translate;
    private notifications;
    private app;
    private colorSchemeProviders;
    profiles: PartialProfile<any>[];
    selectedProfileId: string;
    colorSchemes: TerminalColorScheme[];
    filteredSchemes: TerminalColorScheme[];
    filterText: string;
    loading: boolean;
    savingProfileId: string;
    scrollRegion: ElementRef<HTMLDivElement> | undefined;
    private destroy$;
    private splitFocusSub?;
    constructor(profilesService: ProfilesService, config: ConfigService, sidePanel: SidePanelService, translate: TranslateService, notifications: NotificationsService, app: AppService, colorSchemeProviders: TerminalColorSchemeProvider[]);
    ngOnInit(): void;
    ngOnDestroy(): void;
    get currentProfile(): PartialProfile<any> | undefined;
    init(): Promise<void>;
    loadProfiles(): Promise<void>;
    loadColorSchemes(): Promise<void>;
    onFilterChange(value: string): void;
    applyFilter(): void;
    selectProfile(id: string): void;
    profileLabel(profile: PartialProfile<any>): string;
    private groupLabel;
    applyScheme(scheme: TerminalColorScheme): Promise<void>;
    closePanel(): void;
    private applySchemeToOpenTabs;
    private collectTerminalTabs;
    private setActiveProfileFromFocusedTab;
    private watchSplitFocus;
    private resolveFocusedTab;
    private normalizeSchemeNames;
    private getScrollTop;
    private restoreScroll;
}
