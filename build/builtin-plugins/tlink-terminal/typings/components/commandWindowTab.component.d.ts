import { Injector } from '@angular/core';
import { AppService, BaseTabComponent as CoreBaseTabComponent, BottomPanelService, GetRecoveryTokenOptions, NotificationsService, PlatformService, RecoveryToken } from 'tlink-core';
import { BaseTerminalTabComponent } from '../api/baseTerminalTab.component';
declare const BaseTabComponentRuntime: typeof CoreBaseTabComponent;
type TargetMode = 'active' | 'visible' | 'all';
export declare class CommandWindowTabComponent extends BaseTabComponentRuntime {
    private app;
    private notifications;
    private bottomPanel;
    private platform;
    hostClass: boolean;
    get isTabHost(): boolean;
    get isSplitHost(): boolean;
    get isBottomPanelHost(): boolean;
    inBottomPanel: boolean;
    commandText: string;
    targetMode: TargetMode;
    terminalTargets: BaseTerminalTabComponent<any>[];
    commandAreaHidden: boolean;
    private lastTerminalTab?;
    constructor(app: AppService, notifications: NotificationsService, bottomPanel: BottomPanelService, platform: PlatformService, injector: Injector);
    ngOnInit(): void;
    get connectedCount(): number;
    get targetStatusLabel(): string;
    get canSend(): boolean;
    get editorRows(): number;
    getRecoveryToken(_options?: GetRecoveryTokenOptions): Promise<RecoveryToken>;
    openTargetContextMenu(event: MouseEvent): void;
    private setTargetMode;
    cycleTargetMode(): void;
    get targetModeTooltip(): string;
    clearCommands(): void;
    toggleCommandArea(): void;
    onCommandKeydown(event: KeyboardEvent): void;
    closeWindow(): void;
    sendCommands(): void;
    private setupTerminalTracking;
    private refreshTerminalTargets;
    private getAllTabs;
    private resolveFocusedTab;
    private getActiveTerminalFromApp;
    private getPreferredTerminalTarget;
    private resolveTargets;
    private getVisibleTerminalTargets;
    private normalizeCommands;
    private normalizeScript;
    private detectScriptType;
    private sendScript;
    private wrapScript;
    private getHeredocDelimiter;
    private sendRawToTab;
}
export {};
