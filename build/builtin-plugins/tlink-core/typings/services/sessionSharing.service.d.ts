/// <reference types="node" />
import { Observable } from 'rxjs';
import { LogService } from './log.service';
import { PlatformService } from '../api/platform';
export interface SharedSession {
    id: string;
    terminal: any;
    token: string;
    mode: 'read-only' | 'interactive';
    createdAt: Date;
    expiresAt?: Date;
    password?: string;
    viewers: number;
}
export interface SessionSharingOptions {
    mode: 'read-only' | 'interactive';
    expiresIn?: number;
    password?: string;
}
export interface ParsedShareSessionLink {
    shareUrl: string;
    wsUrl: string;
    sessionId: string;
    token: string;
    transport: 'ws' | 'wss';
    tokenIsLegacyPrefix: boolean;
}
export interface ParsedShareSessionBundleLink {
    shareUrl: string;
    wsUrl: string;
    transport: 'ws' | 'wss';
    sessions: ParsedShareSessionLink[];
}
interface SessionSharingStateChange {
    terminal: any;
    sessionId: string;
    shared: boolean;
}
export declare class SessionSharingService {
    private platform;
    private logger;
    private readonly maxBundleSessions;
    private sharedSessions;
    private terminalToSessionId;
    private sessionIdToTerminal;
    private ws;
    private wsUrl;
    private ipcListenersAttached;
    private sharingStateChanged;
    get sharingStateChanged$(): Observable<SessionSharingStateChange>;
    constructor(log: LogService, platform: PlatformService);
    /**
     * Prompt user to start WebSocket server
     */
    private promptToStartServer;
    /**
     * Generate a shareable link for a terminal session
     */
    shareSession(terminal: any, options: SessionSharingOptions): Promise<string | null>;
    /**
     * Share multiple terminal sessions and return a single bundle URL.
     * Existing shared sessions are reused to avoid interrupting active viewers.
     */
    shareSessionBundle(terminals: any[], options: SessionSharingOptions): Promise<string | null>;
    /**
     * Stop sharing a session
     */
    stopSharing(terminal: any): Promise<void>;
    /**
     * Check if a session is currently shared
     */
    isSessionShared(terminal: any): boolean;
    /**
     * Get the shared session for a terminal
     */
    getSharedSession(terminal: any): SharedSession | null;
    /**
     * Get shareable link for an already-shared terminal
     */
    getShareableLink(terminal: any): Promise<string | null>;
    /**
     * Copy shareable link to clipboard
     */
    copyShareableLink(terminal: any): Promise<boolean>;
    /**
     * Join a shared session
     */
    joinSession(sessionId: string, token: string, password?: string): Promise<boolean>;
    /**
     * Parse tlink session-sharing URL into connection details
     */
    parseShareUrl(rawUrl: string): ParsedShareSessionLink | null;
    /**
     * Parse a multi-session bundle URL generated by shareSessionBundle.
     */
    parseShareBundleUrl(rawUrl: string): ParsedShareSessionBundleLink | null;
    /**
     * Broadcast terminal output to viewers
     */
    broadcastOutput(sessionId: string, data: Buffer): void;
    /**
     * Forward input from viewer to terminal (for interactive mode)
     */
    forwardInput(sessionId: string, data: Buffer): void;
    /**
     * Connect to WebSocket server
     */
    private connectWebSocket;
    /**
     * Handle incoming WebSocket messages
     */
    private handleWebSocketMessage;
    private handleViewerJoined;
    private handleViewerLeft;
    private handleViewerInput;
    /**
     * Get base URL for shareable links
     */
    private getShareBaseInfo;
    /**
     * Get shareable URL with network access information
     */
    getShareableUrlWithInfo(terminal: any): Promise<{
        url: string;
        networkUrl?: string;
        publicUrl?: string;
    } | null>;
    private buildShareUrl;
    private buildShareBundleUrl;
    private convertWebSocketUrlToShareBaseUrl;
    private getTransportFromWebSocketUrl;
    private normalizeEndpoint;
    private isUUID;
    private parseBundlePayload;
    private safeDecodeURIComponent;
    private ensureServerRunningForSharing;
    private ensureSessionRegisteredWithServer;
    private getRemainingExpiryMinutes;
    private extractBundleEntryTitle;
    private attachIPCListeners;
    private handleIncomingViewerInput;
    private normalizeIncomingBuffer;
    /**
     * Get IPC renderer if available (in Electron)
     */
    private getIpcRenderer;
}
export {};
